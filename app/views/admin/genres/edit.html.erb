<div>
  <h2>ジャンル編集</h2>

  <!-- ジャンル編集フォーム -->
  <%= form_with model: [:admin, @genre], local: true do |f| %>
    <div>
      <%= f.label :name, "ジャンル名" %>
      <%= f.text_field :name %>
      <%= f.submit "変更を保存", class: "btn btn-success" %>
    </div>
  <% end %>
</div>